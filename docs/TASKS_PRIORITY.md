# משימות לביצוע – לפי סדר חשיבות

עודכן לפי דו"ח החקירה (8.2.2026).  
**סימון:** ☐ לא בוצע | ☑ בוצע

---

## רמה 1 – קריטי (פונקציונליות חסרה)

משימות שהמשתמש מצפה שהכפתור יעשה משהו – כרגע לא עובד.

### 1.1 SaaS – הוספת פרויקט חדש
**קובץ:** `src/views/SaasView.jsx`  
**חשיבות:** גבוהה מאוד

- [x] **1.1.1** ליצור מודל `AddSaasProjectModal.jsx` (שם, סטטוס, MRR, משימות) או דף טופס.
- [x] **1.1.2** לחבר את כפתור "פרויקט חדש" לפתיחת המודל.
- [x] **1.1.3** להוסיף ב־`AppContext` פונקציה `addSaasProject` (ול־Supabase אם יש סנכרון).
- [x] **1.1.4** לשמור פרויקט חדש עם `id` ייחודי ולעדכן את `saasProjects`.

---

### 1.2 מסחר – צפייה/עריכה/מחיקה של עסקה
**קובץ:** `src/views/TradingView.jsx`  
**חשיבות:** גבוהה

- [x] **1.2.1** ליצור מודל `TradeDetailModal.jsx` (תצוגת פרטי עסקה: תאריך, נכס, אסטרטגיה, מצב רוח, PnL).
- [x] **1.2.2** לחבר את כפתור ChevronRight (או שורת הטבלה) לפתיחת המודל עם העסקה הנבחרת.
- [x] **1.2.3** (אופציונלי) להוסיף עריכה ומחיקה בתוך המודל + פונקציות `updateTrade` / `deleteTrade` ב־`AppContext` (ו־Supabase).

---

### 1.3 Deep Work Timer – כרטיס בדשבורד
**קובץ:** `src/views/DashboardView.jsx`  
**חשיבות:** בינונית–גבוהה (תלוי אם זו פיצ'ר מתוכננת)

- [x] **1.3.1** להחליט: קישור חיצוני לאפליקציה / דף פנימי / טיימר מוטמע.
- [x] **1.3.2** אם קישור – להוסיף `onClick` או `Link` ל־URL. אם טיימר – ליצור קומפוננטת טיימר ולשלב בדשבורד.

---

## רמה 2 – חשוב (חוויית משתמש ועקביות)

### 2.1 גרף 7 ימים – כשאין נתונים
**קובץ:** `src/views/DashboardView.jsx` + אופציונלי `src/components/charts/PnlChart.jsx`

- [x] **2.1.1** כש־`last7DaysChartData` ריק – להציג הודעה: "אין נתונים ל־7 הימים האחרונים" (או fallback ל־7 עסקאות אחרונות עם תווית מתאימה).

---

### 2.2 עקביות שמות – מפתחות אחסון
**קובץ:** `src/data/constants.js`

- [x] **2.2.1** להחליט אם לשנות `rafael_os_*` ל־`refael_os_*` (או להשאיר כ־legacy ולתעד).
- [x] **2.2.2** אם משנים – לטפל ב־migration למי שכבר שמר נתונים (או להשאיר מפתחות ישנים לקריאה).

---

### 2.3 כותרות דפים (נגישות/SEO)
**קבצים:** `src/views/DashboardView.jsx`, `TradingView.jsx`, `SaasView.jsx`, `VisionView.jsx`

- [x] **2.3.1** לוודא שלכל view יש כותרת ראשית אחת ברורה (למשל `h2` עם תוכן תיאורי) למסך הראשי של הדף.

---

## רמה 3 – רצוי (שיפור ואיכות)

### 3.1 Supabase – feedback למשתמש בשגיאות
**קבצים:** `src/context/AppContext.jsx`, `src/lib/supabaseSync.js`

- [x] **3.1.1** במקום רק `console.warn` – להציג הודעה למשתמש (למשל toast או באנר): "שגיאה בסנכרון – הנתונים נשמרו מקומית".
- [ ] **3.1.2** (אופציונלי) להוסיף retry אחרי כישלון שמירה.

---

### 3.2 SEO – Open Graph / Twitter cards
**קובץ:** `index.html` (או meta דרך React)

- [x] **3.2.1** להוסיף meta tags: `og:title`, `og:description`, `og:image` (ואם רלוונטי `twitter:card`).

---

### 3.3 מדיניות סיסמה
**הגדרות Supabase / Auth**

- [ ] **3.3.1** לוודא ב־Supabase Dashboard מדיניות סיסמה (אורך, מורכבות) ולהתאים לציפיות המוצר.

---

## סדר ביצוע מומלץ (לפי עדיפות)

1. **1.1** – SaaS פרויקט חדש (כפתור שכבר קיים, חוסר פונקציה בולט).
2. **1.2** – פרטי/עריכת עסקה (משלים את יומן המסחר).
3. **2.1** – הודעת "אין נתונים" בגרף (שיפור UX מהיר).
4. **1.3** – Deep Work Timer (לפי צורך).
5. **2.3** – כותרות דפים.
6. **2.2** – עקביות מפתחות (אם רוצים לאחד).
7. **3.1** – הודעות שגיאה Supabase.
8. **3.2** – Open Graph (אם יש שיתוף).
9. **3.3** – מדיניות סיסמה (פעם אחת).

---

## סיכום

| רמה   | כמות משימות | תיאור                          |
|-------|-------------|---------------------------------|
| רמה 1 | 3           | פונקציונליות חסרה (כפתורים)   |
| רמה 2 | 3           | חוויית משתמש ועקביות          |
| רמה 3 | 3           | שיפור, SEO, אבטחה/תחזוקה      |

לאחר ביצוע רמה 1 ו־2 האתר יהיה פונקציונלי ועקבי יותר; רמה 3 משפרת איכות ושיווק.
