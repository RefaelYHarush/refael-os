# משימות מומלצות – REFAEL OS (פברואר 2026)

מבוסס על חקירה מעמיקה: קוד, תיעוד קיים (TASK_LIST, WHERE_THE_SITE_FAILS, FIX_FAILURES_TASKS, TASKS_BY_PRIORITY) והמלצות הביקורת.  
**סימון:** ☐ לא בוצע | ☑ בוצע

---

## א. עדיפות גבוהה

### א1. אימות ופריסה

| # | משימה | קובץ/מיקום | הערות |
|---|--------|-------------|--------|
| א1.1 | **הגשת אימות גוגל** – להשלים את הצעדים ב־VERIFICATION_STEP_BY_STEP (סרטון, קישורים, Submit). | אתה + Console | רק אתה יכול להגיש מהחשבון שיצר את הפרויקט. |
| א1.2 | **וידוא RLS ב־Supabase** – לוודא שכל הטבלאות הרלוונטיות מוגנות ב־RLS לפי auth.uid(). | Supabase Dashboard / SQL | ☑ בוצע – כל 9 הטבלאות עם RLS + policy. ראה SUPABASE_VERIFICATION_DONE.md. |
| א1.3 | **Redirect URLs** – לוודא ב־Supabase Authentication רק את refael-os.vercel.app ו־localhost:5173 (אין דומיינים זרים). | Supabase → Auth → URL Configuration | וידוא ידני בדשבורד; הוראות ב־SUPABASE_VERIFICATION_DONE.md. |

### א2. בדיקות ותיעוד

| # | משימה | קובץ/מיקום | הערות |
|---|--------|-------------|--------|
| א2.1 | **הרצת בדיקות** – להריץ `npm install` ואז `npm run test:run` ולוודא שהכל עובר. | טרמינל | ☑ בוצע; נוספו mocks ב־vitest.setup (localStorage, matchMedia) ותיקון Router ב־LandingView.test. |
| א2.2 | **README – בדיקות** – להוסיף משפט: "לפני הרצת בדיקות יש להריץ `npm install`." | README.md | ☑ בוצע. |

---

## ב. עדיפות בינונית

### ב1. חוויית משתמש ועיצוב (מתוך TASK_LIST – אופציונלי)

| # | משימה | קובץ/מיקום | הערות |
|---|--------|-------------|--------|
| ב1.1 | **דף Auth – גרדיאנט/רקע עדין** מאחורי כרטיס ההתחברות (אם עדיין לא מספיק בולט). | AuthView.jsx | א3.1 ב־TASK_LIST. |
| ב1.2 | **טבעת פוקוס (focus-visible)** על כפתור "התחבר עם Google" – לוודא ש־focus-visible:ring-2 מופיע. | AuthView.jsx | נגישות מקלדת. |
| ב1.3 | **עקביות border-radius** – לתעד בפרויקט ערכי radius (כפתורים, כרטיסים) או לאכוף ב־Tailwind. | tailwind.config / docs | א3.4 ב־TASK_LIST. |

### ב2. סנכרון ונתונים

| # | משימה | קובץ/מיקום | הערות |
|---|--------|-------------|--------|
| ב2.1 | **(אופציונלי) Debounce לשמירה ל־Supabase** – כרגע כל שינוי state מפעיל שמירה מיד; אפשר להוסיף debounce (למשל 500–800ms) ל־save* כדי להפחית קריאות. | AppContext.jsx | שיפור ביצועים; לא חובה. |
| ב2.2 | **אינדיקציה "מתעדכן..."** – אופציונלי: להציג ספינר או טקסט קצר בזמן שמירה ראשונה אחרי עריכה (אם רוצים משוב חזותי). | Layout / AppContext | שיפור UX. |

### ב3. תיעוד ועקביות

| # | משימה | קובץ/מיקום | הערות |
|---|--------|-------------|--------|
| ב3.1 | **הערת constants** – ב־constants.js הכותרת אומרת "RAFAEL OS"; לעדכן ל־"Refael OS" או להשאיר ולהסביר שזה legacy. | src/data/constants.js | ☑ בוצע. |
| ב3.2 | **אינדקס תיעוד** – ליצור docs/README.md או לעדכן קובץ קיים עם רשימת מסמכים וקישורים (אימות, פיתוח, פריסה, משפטי). | docs/README.md | ☑ בוצע. |

---

## ג. עדיפות נמוכה

### ג1. משפטי ופרטיות

| # | משימה | קובץ/מיקום | הערות |
|---|--------|-------------|--------|
| ג1.1 | **GDPR (אם יש משתמשים באיחוד האירופי)** – לשקול משפט במדיניות הפרטיות: "עיבוד נתונים בהתאם ל־GDPR לצורך מתן השירות." | PrivacyPage.jsx / docs | LEGAL_PROTECTION_CHECKLIST כבר מציין. |

### ג2. בדיקות ונגישות

| # | משימה | קובץ/מיקום | הערות |
|---|--------|-------------|--------|
| ג2.1 | **בדיקות נוספות** – להוסיף טסט ל־LandingView (כבר קיים LandingView.test.jsx) או ל־AuthView (תצוגה, כפתורים). | src/views/ | כיסוי טוב יותר. |
| ג2.2 | **בדיקת נגישות** – להריץ כלי (למשל axe או Lighthouse) על דף נחיתה ודשבורד ולתקן בעיות קריטיות. | דפדפן / CI | אופציונלי. |

### ג3. PWA ו־SEO

| # | משימה | קובץ/מיקום | הערות |
|---|--------|-------------|--------|
| ג3.1 | **PWA – manifest** – ב־vite.config יש `manifest: false` ו־manifest.json ב־public; לוודא ש־index.html מקשר ל־/manifest.json והתנהגות מתאימה. | index.html, vite.config.js, public/manifest.json | כבר מקושר ב־index. |
| ג3.2 | **דומיין מותאם** – בעת מעבר לדומיין משלך: לעדכן את כל ה־URLs במסמכי האימות, ב־index.html (og:url, og:image) וב־Supabase. | ראה CUSTOM_DOMAIN.md | כשמחליטים על דומיין. |

---

## ד. סיכום לפי קטגוריה

| קטגוריה | כמות | תיאור |
|---------|------|--------|
| א – גבוהה | 5 | אימות גוגל, RLS, Redirect URLs, בדיקות, README |
| ב – בינונית | 8 | Auth עיצוב/פוקוס, radius, debounce, משוב שמירה, constants, אינדקס תיעוד |
| ג – נמוכה | 5 | GDPR, טסטים/נגישות, PWA/דומיין |

**סה"כ:** 18 משימות (חלקן אופציונליות).

---

## ה. סדר ביצוע מומלץ

1. **א1.1** – הגשת אימות גוגל (אם עדיין לא הוגש).  
2. **א1.2, א1.3** – וידוא RLS ו־Redirect URLs ב־Supabase.  
3. **א2.1, א2.2** – הרצת בדיקות + עדכון README.  
4. **ב1.2** – וידוא focus על כפתור Google (מהיר).  
5. **ב3.1, ב3.2** – constants + אינדקס תיעוד.  
6. שאר המשימות לפי צורך וזמן.

---

*מסמך זה משלים את TASK_LIST.md, TASKS_BY_PRIORITY.md, FIX_FAILURES_TASKS.md ו־PROFESSIONAL_AUDIT_AND_RECOMMENDATIONS.md.*
